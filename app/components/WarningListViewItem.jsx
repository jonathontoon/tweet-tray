import React from 'react';
import ListViewItem from './ListViewItem';

const WarningListViewItem = (props) => {
  return (
    <ListViewItem
      {...props}
    />
  );
};

export default WarningListViewItem;

